 <h1 class="index-title">投稿一覧</h1>
<% @catchcopies.each do |catchcopy| %>
<ul>
  <li class="index-content">
   <h2><%= catchcopy.content %></h2>
   <h3><%= catchcopy.book.title%></h3>
   <a href= "/books/<%= catchcopy.book.id%>">書籍の確認</a>
  </li>
</ul> 
<% end %>